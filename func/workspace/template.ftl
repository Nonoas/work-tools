# 修改说明
> ${modifyReason}

/* ${modifyNum}  ${.now?string["yyyy/MM/dd"]}  huangsc <#if modifyReason?length gte 20>${modifyReason?substring(0,20)} <#else>${modifyReason}</#if>  ${versionNO} beg */

/* ${modifyNum}  ${.now?string["yyyy/MM/dd"]}  huangsc <#if modifyReason?length gte 20>${modifyReason?substring(0,20)} <#else>${modifyReason}</#if>  ${versionNO} end */

# 修改方案

# 调试数据

# 修改内容

# 提交日志
[需求编号] ${reqNums!}
[修改单编号] ${modifyNum!}
[修改单版本] ${versionNO!}
[需求引入行]
[需求描述] ${modifyReason!}